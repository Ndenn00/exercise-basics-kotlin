package com.basics.india

// unexpected null passing will result in compiler error

// to allow nulls, '?' has to come after the type declaration

// ?. these are safe calls
// checks for null first, then stops the method call for nulls
// no need for null checks

// elvis operator / null coalescing operator
// assign new objects

// safe cast  - as?
// casts to type or returns null

// not null assertion - !!
// very blunt, if we know that the type will never be null
// if it is null, throw a null pointer at the assignment/assertion point


